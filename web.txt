<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<meta name="color-scheme" content="dark"/>
<title>Sarvajña · SkillOS — Adaptive Exam Prep</title>
<link rel="preconnect" href="https://fonts.googleapis.com"/>
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin/>
<link href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,400;9..144,500;9..144,600&family=Inter:wght@400;500;600&display=swap" rel="stylesheet"/>
<style>
/* ============ TOKENS ============ */
:root{
  --bg:#0a0a0a; --surface:#101010; --surface2:#151515;
  --border:#232323; --border-2:#333;
  --text:#ececec; --t2:#9c9c9c; --t3:#6e6e6e; --t4:#4a4a4a;
  --fill:#e9e9e9; --fill-ink:#0a0a0a;
  --serif:'Fraunces',Georgia,serif; --sans:'Inter',sans-serif;
}
*{margin:0;padding:0;box-sizing:border-box}
body{background:var(--bg);color:var(--text);font:400 15px/1.6 var(--sans);-webkit-font-smoothing:antialiased;min-height:100vh}
button{font-family:inherit;cursor:pointer;background:none;border:none;color:inherit}
input{font-family:inherit}
:focus-visible{outline:1px solid var(--t2);outline-offset:2px}
::-webkit-scrollbar{width:10px;height:10px}
::-webkit-scrollbar-thumb{background:#242424;border-radius:0}
::-webkit-scrollbar-track{background:transparent}
::selection{background:#e9e9e9;color:#0a0a0a}

/* ============ PRIMITIVES ============ */
.ol{font:600 10.5px/1 var(--sans);letter-spacing:1.8px;text-transform:uppercase;color:var(--t3)}
.serif{font-family:var(--serif)}
.btn{display:inline-flex;align-items:center;justify-content:center;gap:8px;font:500 13.5px/1 var(--sans);
  padding:12px 22px;border-radius:2px;transition:background .15s,color .15s,border-color .15s;white-space:nowrap}
.btn-primary{background:var(--fill);color:var(--fill-ink);border:1px solid var(--fill)}
.btn-primary:hover{background:#fff;border-color:#fff}
.btn-primary:disabled{background:#161616;color:var(--t4);border-color:#1c1c1c;cursor:not-allowed}
.btn-ghost{border:1px solid var(--border);color:var(--t2)}
.btn-ghost:hover{color:var(--text);border-color:var(--border-2)}
.btn-text{color:var(--t2);font:500 13px var(--sans);text-decoration:underline;text-underline-offset:3px;text-decoration-color:#3a3a3a;padding:2px}
.btn-text:hover{color:var(--text)}
.tag{font:600 10px/1 var(--sans);letter-spacing:1.4px;text-transform:uppercase;padding:5px 9px;border:1px solid var(--border-2);color:var(--t2);border-radius:2px}
.tag.solid{border-color:#d9d9d9;color:var(--text)}
.rule{border:none;border-top:1px solid var(--border);margin:0}

/* ============ SCREENS ============ */
.screen{display:none;min-height:100vh}
.screen.active{display:block;animation:enter .3s ease}
@keyframes enter{from{opacity:0;transform:translateY(6px)}to{opacity:1;transform:none}}

/* ============ LOGIN ============ */
#screen-login.active{display:flex;align-items:center;justify-content:center;padding:32px 20px}
.auth{width:min(400px,100%)}
.wordmark{font:500 40px/1 var(--serif);letter-spacing:.5px;text-align:center}
.wordsub{font:600 10px/1 var(--sans);letter-spacing:3px;text-transform:uppercase;color:var(--t3);text-align:center;margin:14px 0 38px}
.tabs{display:flex;gap:28px;border-bottom:1px solid var(--border);margin-bottom:28px}
.tab{padding:0 2px 12px;font:500 13.5px var(--sans);color:var(--t3);border-bottom:1px solid transparent;margin-bottom:-1px;transition:color .15s}
.tab:hover{color:var(--t2)}
.tab.on{color:var(--text);border-bottom-color:#e9e9e9}
.f{margin-bottom:18px}
.f label{display:block;font:600 10.5px/1 var(--sans);letter-spacing:1.6px;text-transform:uppercase;color:var(--t3);margin-bottom:9px}
.f .in{position:relative}
.f input{width:100%;background:transparent;border:1px solid var(--border);border-radius:2px;color:var(--text);
  font:400 14.5px var(--sans);padding:12px 14px;transition:border-color .15s}
.f input::placeholder{color:#3e3e3e}
.f input:focus{outline:none;border-color:#5a5a5a}
.pw input{padding-right:66px}
.pw .peek{position:absolute;right:6px;top:50%;transform:translateY(-50%);font:500 12px var(--sans);color:var(--t3);
  padding:6px 10px;border-radius:2px}
.pw .peek:hover{color:var(--text)}
.frow{display:flex;justify-content:space-between;align-items:center;margin:2px 0 22px}
.cbx{display:flex;align-items:center;gap:9px;font:400 13px var(--sans);color:var(--t2);cursor:pointer;user-select:none}
.cbx input{display:none}
.cbx .b{width:15px;height:15px;border:1px solid var(--border-2);border-radius:2px;display:grid;place-items:center;transition:.15s}
.cbx .b::after{content:'';width:7px;height:3px;border-left:1.5px solid var(--fill-ink);border-bottom:1.5px solid var(--fill-ink);transform:rotate(-45deg) translate(0,-1px);opacity:0}
.cbx input:checked + .b{background:var(--fill);border-color:var(--fill)}
.cbx input:checked + .b::after{opacity:1}
.fnote{border-left:2px solid var(--border-2);padding:8px 0 8px 14px;font:400 13px var(--sans);color:var(--t2);margin-bottom:18px;display:none}
.fnote.show{display:block}
.alt{margin-top:30px;padding-top:24px;border-top:1px solid var(--border);text-align:center;font:400 13px var(--sans);color:var(--t3)}
.quote{margin-top:34px;text-align:center;font:italic 400 14px var(--serif);color:var(--t3)}

/* ============ ONBOARDING ============ */
#screen-onboarding.active{display:flex;align-items:center;justify-content:center;padding:36px 20px}
.ob{width:min(600px,100%)}
.ob-top{display:flex;justify-content:space-between;align-items:baseline;margin-bottom:16px}
.ob-brand{font:500 17px var(--serif)}
.ob-step{font:600 10.5px var(--sans);letter-spacing:2px;text-transform:uppercase;color:var(--t3)}
.ob-bar{height:1px;background:#1e1e1e;margin-bottom:6px;position:relative}
.ob-bar i{position:absolute;left:0;top:-0.5px;height:2px;background:#d9d9d9;transition:width .35s ease}
.ob-dots{display:flex;gap:8px;margin:16px 0 30px}
.ob-dots i{width:4px;height:4px;border-radius:50%;background:#2c2c2c}
.ob-dots i.on{background:#d9d9d9}
.ob-q{font:500 27px/1.25 var(--serif);margin-bottom:8px}
.ob-s{color:var(--t3);font-size:13.5px;margin-bottom:28px}
.opts{display:grid;gap:10px}
.opts.two{grid-template-columns:1fr 1fr}
.opts.three{grid-template-columns:1fr 1fr 1fr}
.opt{display:flex;align-items:center;gap:14px;padding:15px 18px;border:1px solid var(--border);border-radius:2px;
  color:var(--t2);font:500 14px var(--sans);text-align:left;transition:border-color .15s,color .15s,background .15s}
.opt:hover{border-color:var(--border-2);color:var(--text)}
.opt .sq{width:15px;height:15px;flex:none;border:1px solid #3a3a3a;border-radius:2px;display:grid;place-items:center;transition:.15s}
.opt .sq::after{content:'';width:7px;height:3px;border-left:1.5px solid var(--fill-ink);border-bottom:1.5px solid var(--fill-ink);transform:rotate(-45deg) translate(0,-1px);opacity:0}
.opt.sel{border-color:#cfcfcf;background:#141414;color:var(--text)}
.opt.sel .sq{background:var(--fill);border-color:var(--fill)}
.opt.sel .sq::after{opacity:1}
.ob-foot{display:flex;justify-content:space-between;align-items:center;margin-top:26px}
.ob-back{font:500 13px var(--sans);color:var(--t3);padding:6px 2px}
.ob-back:hover{color:var(--text)}
.slider-w{text-align:center;padding:14px 0 6px}
.sl-v{font:500 58px/1 var(--serif)}
.sl-w{font:600 10.5px var(--sans);letter-spacing:2px;text-transform:uppercase;color:var(--t3);margin:10px 0 26px}
input[type=range]{width:100%;max-width:330px;accent-color:#d9d9d9;height:4px}
.ob-done{text-align:center;padding:44px 0 30px}
.ob-done .em{font:500 22px var(--serif);margin-top:26px}
.ofl{margin-top:24px;display:grid;gap:9px;justify-items:center}
.ofl div{color:var(--t3);font:400 13.5px var(--sans);opacity:0;animation:enter .4s ease forwards}
.ofl b{color:var(--t2);font-weight:500}

/* ============ TOP NAV ============ */
.topnav{position:sticky;top:0;z-index:40;background:rgba(10,10,10,.92);backdrop-filter:blur(8px);
  border-bottom:1px solid var(--border)}
.nav-in{max-width:1080px;margin:0 auto;display:flex;align-items:center;gap:30px;padding:0 28px;height:58px}
.brand{display:flex;align-items:baseline;gap:10px;flex:none}
.brand b{font:500 18px var(--serif)}
.brand span{font:600 9px var(--sans);letter-spacing:2.5px;color:var(--t3)}
.nav-links{display:flex;gap:24px;overflow-x:auto;scrollbar-width:none}
.nav-links::-webkit-scrollbar{display:none}
.nl{font:500 13px var(--sans);color:var(--t3);padding:4px 0;white-space:nowrap;transition:color .15s}
.nl:hover{color:var(--t2)}
.nl.on{color:var(--text);text-decoration:underline;text-underline-offset:6px;text-decoration-thickness:1px}
.nav-meta{margin-left:auto;display:flex;align-items:center;gap:18px;flex:none}
.nm{font:400 12.5px var(--sans);color:var(--t3);white-space:nowrap}
.nm b{color:var(--t2);font-weight:500}
.nm.panic{border:1px solid var(--border-2);border-radius:2px;padding:7px 13px;color:var(--t2);font-weight:500}
.nm.panic:hover{color:var(--text);border-color:#4a4a4a}
@media(max-width:900px){.nm.hide-m{display:none}}
@media(max-width:640px){.nav-in{padding:0 16px;gap:16px}.brand span{display:none}}

/* ============ VIEWS ============ */
.view{display:none;max-width:860px;margin:0 auto;padding:40px 28px 100px}
.view.active{display:block;animation:enter .3s ease}
.vh{margin-bottom:34px}
.vh h1{font:500 30px/1.2 var(--serif)}
.vh p{color:var(--t3);font-size:13.5px;margin-top:8px}

/* --- flow --- */
.flow{border:1px solid var(--border);border-radius:2px;padding:20px 24px;margin-bottom:30px}
.flow-line{display:flex;align-items:center;gap:14px;margin-top:16px;flex-wrap:wrap}
.fs{display:flex;align-items:center;gap:10px}
.fn{width:24px;height:24px;border:1px solid #2a2a2a;border-radius:50%;display:grid;place-items:center;
  font:500 11.5px var(--sans);color:var(--t4);flex:none}
.fs .fl{font:500 13px var(--sans);color:var(--t4)}
.fs.cur .fn{border-color:#d9d9d9;color:var(--text)}
.fs.cur .fl{color:var(--text)}
.fs.done .fn{border-color:#2a2a2a;background:#181818;color:var(--t3)}
.fs.done .fl{color:var(--t3);text-decoration:line-through;text-decoration-color:#3a3a3a}
.fc{flex:1;min-width:20px;height:1px;background:#222}
.here{font:400 12px var(--sans);color:var(--t3);font-style:italic}
.go-link{font:500 12.5px var(--sans);color:var(--t2);text-decoration:underline;text-underline-offset:3px}
.go-link:hover{color:var(--text)}

/* --- greeting / blocks --- */
.greet{margin-bottom:10px}
.greet h2{font:500 25px var(--serif)}
.greet p{color:var(--t3);font-size:13.5px;margin-top:5px}
.lvl-note{color:var(--t3);font:italic 400 13px var(--serif);margin-top:6px;display:none}
.lvl-note.show{display:block;animation:enter .5s ease}
.block{border:1px solid var(--border);border-radius:2px;padding:26px 28px;margin-top:16px}
.block.now{padding:30px 32px}
.block h3{font:500 22px/1.3 var(--serif);margin:10px 0 6px}
.bmeta{font:400 13px var(--sans);color:var(--t3)}
.bwhy{font:400 13.5px var(--sans);color:var(--t2);margin-top:14px}
.bwhy b{color:var(--text);font-weight:500}
.bdone{border-left:2px solid #2c2c2c;padding:4px 0 4px 16px;margin:16px 0 22px;font:400 13.5px var(--sans);color:var(--t2)}
.bdone span{font:600 10.5px var(--sans);letter-spacing:1.6px;text-transform:uppercase;color:var(--t3);display:block;margin-bottom:3px}
.bacts{display:flex;gap:12px;flex-wrap:wrap}
.brow{display:flex;align-items:center;gap:16px;border:1px solid var(--border);border-radius:2px;
  padding:15px 20px;margin-top:10px}
.brow .ol{width:52px;flex:none}
.brow b{font:500 14.5px var(--sans);display:block}
.brow i{font:400 12.5px var(--sans);color:var(--t3);font-style:normal}
.brow.later{opacity:.55}
.brow .st{margin-left:auto;font:400 12px var(--sans);color:var(--t4);flex:none}
.donetoday{margin-top:34px}
.dt-row{display:flex;align-items:baseline;gap:12px;padding:9px 0;border-bottom:1px solid #171717;font:400 13.5px var(--sans)}
.dt-row b{font-weight:400;color:var(--t2)}
.dt-row i{font-style:normal;color:var(--t4);font-size:12.5px;margin-left:auto}
.allclear{border:1px solid var(--border);border-radius:2px;padding:44px 36px;text-align:center;margin-top:16px}
.allclear h3{font:500 23px var(--serif);margin-bottom:10px}
.allclear p{color:var(--t3);font-size:13.5px;max-width:430px;margin:0 auto 24px}
.qrow{display:flex;gap:14px;margin-top:28px;flex-wrap:wrap}

/* --- bringe --- */
.cd{display:flex;align-items:baseline;gap:16px;border-bottom:1px solid var(--border);padding-bottom:26px;margin-bottom:8px;flex-wrap:wrap}
.cd b{font:500 44px/1 var(--serif)}
.cd span{font:400 13px var(--sans);color:var(--t3)}
.cd .cd-acts{margin-left:auto;display:flex;gap:10px}
@media(max-width:700px){.cd .cd-acts{margin-left:0}}
.sect{border-top:1px solid var(--border);padding:30px 0 34px}
.sect:first-of-type{border-top:none}
.sect h2{font:500 20px var(--serif);margin:10px 0 6px}
.sect .desc{font:400 13.5px var(--sans);color:var(--t3);margin-bottom:22px;max-width:560px}
.snum{font:400 13px var(--serif);color:var(--t4)}
.dz{border:1px dashed #333;border-radius:2px;padding:34px 20px;text-align:center;cursor:pointer;transition:border-color .15s}
.dz:hover{border-color:#4a4a4a}
.dz b{display:block;font:500 14.5px var(--sans)}
.dz span{font:400 12.5px var(--sans);color:var(--t3)}
.dz.busy{pointer-events:none;border-style:solid;border-color:#2c2c2c}
.dz.ok{border-style:solid;border-color:#3d3d3d;cursor:default}
.dz-sub{text-align:center;margin-top:12px}
.status{font:400 13px var(--sans);color:var(--t2);margin-top:16px}
.file-ok{display:inline-flex;gap:10px;align-items:baseline;font:400 13.5px var(--sans);color:var(--t2)}
.file-ok b{font-weight:500;color:var(--text)}
/* pass core */
.conf{display:flex;align-items:center;gap:20px;margin-bottom:28px}
.conf .num{font:500 40px/1 var(--serif)}
.conf .cbar{flex:1;max-width:220px;height:1px;background:#262626;position:relative}
.conf .cbar i{position:absolute;left:0;top:-0.5px;height:2px;background:#d9d9d9}
.conf p{font:400 12.5px var(--sans);color:var(--t3)}
.pc{display:flex;gap:18px;align-items:baseline;padding:15px 0;border-bottom:1px solid #171717}
.pc:first-of-type{border-top:1px solid #171717}
.pc .rk{font:400 13px var(--serif);color:var(--t4);width:24px;flex:none}
.pc .m{flex:1;min-width:0}
.pc .m b{font:500 14.5px var(--sans);display:block}
.pc .m span{font:400 12.5px var(--sans);color:var(--t3)}
.pc .sd{text-align:right;display:grid;gap:6px;justify-items:end;flex:none}
.pc .sd i{font:400 12px var(--sans);color:var(--t3);font-style:normal}
@media(max-width:560px){.pc{flex-wrap:wrap}.pc .sd{flex-direction:row;display:flex;gap:12px;text-align:left;justify-content:start}}
.tl{display:flex;gap:18px;align-items:baseline;padding:11px 0;border-bottom:1px solid #151515}
.tl .t{width:70px;flex:none;font:400 12.5px var(--sans);color:var(--t3);text-align:right}
.tl .b{flex:1}
.tl .b b{font:500 14px var(--sans);display:block}
.tl .b span{font:400 12.5px var(--sans);color:var(--t3)}
.tl.protect{background:#0f0f0f;padding:13px 12px;margin:2px -12px;border-bottom:none}
.tl.protect .b b{color:var(--text)}
.honesty{font:italic 400 13px var(--serif);color:var(--t3);text-align:center;margin-top:34px}
.startline{text-align:center;margin-top:28px}

/* --- path --- */
.pathwrap{overflow-x:auto;padding:34px 0 20px;margin:0 -8px}
.path{display:flex;gap:44px;min-width:max-content;padding:0 28px;position:relative;align-items:flex-start}
.path::before{content:'';position:absolute;left:40px;right:40px;top:22px;border-top:1px solid #1f1f1f}
.isle{position:relative;width:120px;cursor:pointer;text-align:center}
.node{width:44px;height:44px;margin:0 auto 14px;border-radius:50%;display:grid;place-items:center;
  font:500 13px var(--sans);position:relative;background:var(--bg);transition:border-color .15s}
.node{border:1px solid #2a2a2a;color:var(--t4)}
.isle:hover .node{border-color:#4a4a4a}
.node.cur{border-color:#d9d9d9;color:var(--text)}
.node.done{background:#e0e0e0;border-color:#e0e0e0}
.node.done::after{content:'';width:11px;height:5px;border-left:1.5px solid #0a0a0a;border-bottom:1.5px solid #0a0a0a;transform:rotate(-45deg) translate(0,-1px)}
.node.lock{border-style:dashed;border-color:#262626;color:#3d3d3d}
.isle b{display:block;font:500 13.5px var(--sans)}
.isle i{display:block;font:400 11.5px var(--sans);color:var(--t4);font-style:normal;margin:3px 0 5px}
.isle em{font:600 9.5px var(--sans);letter-spacing:1.4px;text-transform:uppercase;font-style:normal;color:var(--t4)}
.isle.done em,.isle.cur em{color:var(--t2)}
.pathnote{font:400 13px var(--sans);color:var(--t3);margin-top:8px}

/* --- settings --- */
.set{border-top:1px solid var(--border);padding:28px 0}
.set:first-child{border-top:none;padding-top:0}
.set h2{font:500 19px var(--serif);margin:8px 0 4px}
.set .d{font:400 13.5px var(--sans);color:var(--t3);margin-bottom:20px}
.kv{display:flex;align-items:baseline;font:400 14px var(--sans)}
.kv span{color:var(--t2);white-space:nowrap}
.kv .dots{flex:1;border-bottom:1px dotted #333;margin:0 10px;transform:translateY(-4px)}
.kv b{font-weight:500;color:var(--text);white-space:nowrap}
.set .acts{display:flex;gap:12px;flex-wrap:wrap;margin-top:6px}

/* ============ DRAWER ============ */
.dw{position:fixed;inset:0;z-index:60;pointer-events:none}
.dw.open{pointer-events:auto}
.dw-bg{position:absolute;inset:0;background:rgba(0,0,0,.65);opacity:0;transition:opacity .25s}
.dw.open .dw-bg{opacity:1}
.dw-panel{position:absolute;top:0;right:0;bottom:0;width:min(400px,94vw);background:var(--bg);
  border-left:1px solid var(--border);transform:translateX(102%);transition:transform .3s cubic-bezier(.3,1,.3,1);
  padding:34px 30px;overflow-y:auto}
.dw.open .dw-panel{transform:none}
.dw-x{position:absolute;top:20px;right:22px;font:400 12px var(--sans);color:var(--t3);letter-spacing:1px}
.dw-x:hover{color:var(--text)}
.dw h3{font:500 23px var(--serif);margin:6px 0 4px}
.dw .st{margin-bottom:26px}
.dwsec{margin-bottom:24px}
.dwsec h4{font:600 10.5px var(--sans);letter-spacing:1.8px;text-transform:uppercase;color:var(--t3);margin-bottom:12px}
.dwrow{display:flex;align-items:center;gap:12px;padding:10px 0;border-bottom:1px solid #161616;font:400 13.5px var(--sans);color:var(--t2)}
.dwrow .ck{width:14px;height:14px;flex:none;border:1px solid #3a3a3a;border-radius:2px;display:grid;place-items:center;transition:.15s}
.dwrow .ck::after{content:'';width:6px;height:3px;border-left:1.5px solid var(--fill-ink);border-bottom:1.5px solid var(--fill-ink);transform:rotate(-45deg);opacity:0}
.dwrow.on .ck{background:var(--fill);border-color:var(--fill)}
.dwrow.on .ck::after{opacity:1}
.dwrow.on{color:var(--text)}
button.dwrow{width:100%;text-align:left;transition:color .15s}
button.dwrow:hover{color:var(--text)}
.dwrow .eff{font:400 13px var(--sans);color:var(--t2)}
.dwrow .eff b{font-weight:500;color:var(--text)}
.dwnote{border-left:2px solid #2c2c2c;padding:4px 0 4px 14px;font:400 13px var(--sans);color:var(--t3)}
.dw .btn{width:100%;margin-top:6px}

/* ============ SHEET (modal) ============ */
.sh{position:fixed;inset:0;z-index:70;display:none;align-items:center;justify-content:center;padding:24px}
.sh.open{display:flex}
.sh-bg{position:absolute;inset:0;background:rgba(0,0,0,.75)}
.sh-panel{position:relative;width:min(780px,100%);max-height:86vh;overflow-y:auto;background:var(--surface);
  border:1px solid var(--border);border-radius:2px;padding:36px 38px;animation:enter .3s ease}
.sh-head{display:flex;align-items:baseline;gap:18px;border-bottom:1px solid var(--border);padding-bottom:22px;margin-bottom:26px}
.sh-head h2{font:500 24px var(--serif)}
.sh-head p{font:400 12.5px var(--sans);color:var(--t3)}
.sh-acts{margin-left:auto;display:flex;gap:10px;flex:none}
.sh-grid{display:grid;grid-template-columns:1fr 1fr;gap:30px 36px}
.sh-card h3{font:500 16px var(--serif);margin-bottom:12px;padding-bottom:9px;border-bottom:1px solid #1d1d1d}
.sh-card ul{list-style:none}
.sh-card li{font:400 13px/1.65 var(--sans);color:var(--t2);padding:4px 0 4px 16px;position:relative}
.sh-card li::before{content:'—';position:absolute;left:0;color:var(--t4)}
.sh-card li b{color:var(--text);font-weight:500}
.sh-card.full{grid-column:1/-1;border:1px solid var(--border);padding:22px 24px;border-radius:2px}
.sh-card.full h3{border-bottom:none;padding-bottom:0;margin-bottom:10px}
@media(max-width:640px){.sh-grid{grid-template-columns:1fr}.sh-panel{padding:26px 22px}}

/* ============ PANIC ============ */
.pn{position:fixed;inset:0;z-index:80;display:none;place-items:center;background:#050505}
.pn.open{display:grid;animation:enter .35s ease}
.pn-x{position:absolute;top:26px;right:30px;font:400 12px var(--sans);color:var(--t3);letter-spacing:1px}
.pn-x:hover{color:var(--text)}
.pn-in{text-align:center;padding:20px}
.ring{width:270px;height:270px;margin:0 auto 40px;position:relative;display:grid;place-items:center;border:1px solid #1c1c1c;border-radius:50%}
.ring::after{content:'';position:absolute;inset:36px;border:1px dashed #1a1a1a;border-radius:50%}
.core{width:168px;height:168px;border-radius:50%;border:1px solid #3a3a3a;display:grid;place-content:center;gap:8px;
  transition:transform 4s cubic-bezier(.45,.05,.55,.95)}
.core.grow{transform:scale(1.3)}
.core.shrink{transform:scale(.97)}
#ph-label{font:500 17px var(--serif);color:var(--t2)}
#ph-count{font:400 32px var(--serif);color:var(--text)}
.pn-msg{font:italic 400 17px var(--serif);color:var(--t2);max-width:400px;margin:0 auto 10px}
.pn-cy{font:600 10px var(--sans);letter-spacing:2.5px;text-transform:uppercase;color:var(--t4);margin-bottom:34px}
.pn-acts{display:flex;gap:14px;justify-content:center;flex-wrap:wrap}

@media(max-width:640px){
  .view{padding:28px 16px 90px}
  .block.now{padding:24px 20px}
  .opts.two,.opts.three{grid-template-columns:1fr}
}
@media (prefers-reduced-motion:reduce){*{animation-duration:.01ms!important;transition-duration:.01ms!important}}
@media print{
  body *{visibility:hidden}
  #sheet,#sheet *{visibility:visible}
  #sheet{position:absolute;inset:0;display:block!important;background:#fff;border:none}
  #sheet .sh-bg,#sheet .sh-acts{display:none}
  .sh-panel{background:#fff;border:none;max-height:none;overflow:visible}
  .sh-head h2,.sh-head p,.sh-card h3,.sh-card li,.sh-card li b{color:#000!important;-webkit-print-color-adjust:exact}
}
</style>
</head>
<body>

<!-- ================= LOGIN ================= -->
<section id="screen-login" class="screen active">
  <div class="auth">
    <div class="wordmark">Sarvajña</div>
    <div class="wordsub">Adaptive Study · Exam-Eve Rescue</div>

    <div class="tabs">
      <button class="tab on" data-tab="signin" type="button">Sign In</button>
      <button class="tab" data-tab="signup" type="button">Create Account</button>
    </div>

    <form id="auth-form" novalidate>
      <div class="f" id="f-name" hidden>
        <label for="in-name">Full name</label>
        <div class="in"><input id="in-name" type="text" placeholder="Alex Chen" autocomplete="name"/></div>
      </div>
      <div class="f">
        <label for="in-email">Email</label>
        <div class="in"><input id="in-email" type="email" placeholder="you@campus.edu" autocomplete="email"/></div>
      </div>
      <div class="f pw">
        <label for="in-pass">Password</label>
        <div class="in">
          <input id="in-pass" type="password" placeholder="At least 6 characters"/>
          <button type="button" class="peek" id="peek">Show</button>
        </div>
      </div>
      <div class="frow">
        <label class="cbx"><input type="checkbox" id="remember" checked/><span class="b"></span>Remember me</label>
        <button type="button" class="btn-text" id="forgot">Forgot password?</button>
      </div>
      <div class="fnote" id="fnote"></div>
      <button class="btn btn-primary" id="auth-go" type="submit" style="width:100%">Sign In</button>
    </form>

    <form id="fp-form" hidden novalidate>
      <div class="f">
        <label for="fp-email">Account email</label>
        <div class="in"><input id="fp-email" type="email" placeholder="you@campus.edu"/></div>
      </div>
      <div class="fnote" id="fp-note"></div>
      <div style="display:flex;gap:12px">
        <button class="btn btn-primary" type="submit" style="flex:1">Send reset link</button>
        <button class="btn btn-ghost" type="button" id="fp-back">Back</button>
      </div>
    </form>

    <div class="alt">First time here? <button class="btn-text" id="demo">Open the demo student — Alex Chen, CS302</button></div>
    <p class="quote">“Same exam. Different student. Different plan.”</p>
  </div>
</section>

<!-- ================= ONBOARDING ================= -->
<section id="screen-onboarding" class="screen">
  <div class="ob">
    <div class="ob-top"><div class="ob-brand">Sarvajña</div><div class="ob-step" id="ob-step">Calibration · Step 1 of 7</div></div>
    <div class="ob-bar"><i id="ob-fill"></i></div>
    <div class="ob-dots" id="ob-dots"></div>
    <div id="ob-body"></div>
  </div>
</section>

<!-- ================= APP ================= -->
<div id="screen-app" class="screen">
  <header class="topnav">
    <div class="nav-in">
      <div class="brand"><b>Sarvajña</b><span>SKILLOS</span></div>
      <nav class="nav-links">
        <button class="nl on" data-view="dashboard">Dashboard</button>
        <button class="nl" data-view="bringe">Bringe Study</button>
        <button class="nl" data-view="path">Study Path</button>
        <button class="nl" data-view="settings">Settings</button>
      </nav>
      <div class="nav-meta">
        <span class="nm hide-m" id="nm-course">CS302 · Databases</span>
        <span class="nm hide-m">Pass <b id="nm-conf">74%</b></span>
        <span class="nm hide-m">Lv <b id="nm-level">4</b> · <b id="nm-xp">320</b> XP</span>
        <button class="nm panic" data-panic>Panic Mode</button>
      </div>
    </div>
  </header>

  <!-- DASHBOARD -->
  <section class="view active" id="view-dashboard">
    <div class="greet">
      <h2 id="greet">Good evening, Alex</h2>
      <p id="greet-sub"></p>
      <p class="lvl-note" id="lvl-note"></p>
    </div>
    <div class="flow">
      <div class="ol">How SkillOS works</div>
      <div class="flow-line" id="flow-line"></div>
    </div>
    <div id="blocks"></div>
    <div class="donetoday" id="donetoday"></div>
    <div class="qrow">
      <button class="btn btn-ghost" data-sheet>Open Rescue Sheet</button>
      <button class="btn btn-ghost" data-panic>I'm overwhelmed</button>
    </div>
  </section>

  <!-- BRINGE -->
  <section class="view" id="view-bringe">
    <div class="vh"><h1>Bringe Study</h1><p>Exam-eve rescue. Three steps — we cut topics, never sleep.</p></div>
    <div class="cd">
      <b id="cd-time">—</b>
      <span>until the exam<br/>Tomorrow · 9:00 AM · CS302</span>
      <div class="cd-acts">
        <button class="btn btn-ghost" data-sheet>Rescue Sheet</button>
        <button class="btn btn-ghost" data-panic>Panic Mode</button>
      </div>
    </div>

    <div class="sect">
      <div class="snum">01</div>
      <h2>Add your material</h2>
      <p class="desc">Syllabus, past papers, or class notes — PDF, photo, or plain text. Nothing leaves this device in demo mode.</p>
      <div class="dz" id="dz">
        <div id="dz-idle"><b>Drop a file here, or click to browse</b><span>PDF · PNG · JPG · TXT · MD</span></div>
      </div>
      <div class="dz-sub"><button class="btn-text" id="demo-syl">No files handy? Use the demo syllabus instead</button></div>
      <input type="file" id="dz-input" hidden/>
    </div>

    <div class="sect" id="sect2">
      <div class="snum">02</div>
      <h2>Build your rescue plan</h2>
      <p class="desc">Every topic is scored against past papers. Only the Pass Core survives — the smallest set of topics that clears the bar.</p>
      <button class="btn btn-primary" id="build" disabled>Generate Rescue Plan</button>
      <p class="status" id="build-status"></p>
    </div>

    <div class="sect" id="result" hidden>
      <div class="snum">03</div>
      <h2>Pass Core &amp; timeline</h2>
      <div class="conf">
        <div class="num">74%</div>
        <div class="cbar"><i style="width:74%"></i></div>
        <p>Pass Confidence — an estimate, range 62–84%.<br/>Built from past-paper frequency, your recall, and protected sleep. Never a guarantee.</p>
      </div>
      <div id="pcore"></div>
      <hr class="rule" style="margin:34px 0 22px"/>
      <div class="ol" style="margin-bottom:14px">Tonight</div>
      <div id="tline"></div>
      <p class="honesty">If time runs short, we cut content — never sleep. That is the contract.</p>
      <div class="startline"><button class="btn btn-primary" data-goto="dashboard">Start studying — the first block is waiting</button></div>
    </div>
  </section>

  <!-- PATH -->
  <section class="view" id="view-path">
    <div class="vh"><h1>Study Path</h1><p>Your route through CS302. One island at a time.</p></div>
    <div class="pathwrap"><div class="path" id="path"></div></div>
    <p class="pathnote">Click any island for its prerequisites, key concepts, and effort estimate.</p>
  </section>

  <!-- SETTINGS -->
  <section class="view" id="view-settings">
    <div class="vh"><h1>Settings</h1><p>Your Learning Twin, your data, your rules.</p></div>
    <div class="set">
      <div class="ol">Learning Twin</div>
      <h2>How SkillOS models you</h2>
      <p class="d">It sharpens quietly after every study session.</p>
      <div id="twin"></div>
    </div>
    <div class="set">
      <div class="ol">Data &amp; privacy</div>
      <h2>Your data stays yours</h2>
      <p class="d">Export or erase everything, at any time.</p>
      <div class="acts">
        <button class="btn btn-ghost" id="dl">Download initial data (.txt)</button>
        <button class="btn btn-ghost" id="del">Delete my Twin</button>
      </div>
    </div>
    <div class="set">
      <div class="ol">Session</div>
      <h2>Recalibrate or sign out</h2>
      <p class="d">Re-run the seven-step assessment, or leave cleanly.</p>
      <div class="acts">
        <button class="btn btn-ghost" id="recal">Recalibrate assessment</button>
        <button class="btn btn-ghost" id="out">Log out</button>
      </div>
    </div>
  </section>
</div>

<!-- ================= DRAWER ================= -->
<div class="dw" id="dw">
  <div class="dw-bg" id="dw-bg"></div>
  <aside class="dw-panel" id="dw-panel"></aside>
</div>

<!-- ================= RESCUE SHEET ================= -->
<div class="sh" id="sheet">
  <div class="sh-bg" data-sheet-close></div>
  <div class="sh-panel">
    <div class="sh-head">
      <div><h2>Rescue Sheet — CS302</h2><p>Dense, high-yield, built from your own material. A last-hours revision aid — not for the exam hall.</p></div>
      <div class="sh-acts">
        <button class="btn btn-ghost" id="print">Save as PDF</button>
        <button class="btn btn-ghost" data-sheet-close>Close</button>
      </div>
    </div>
    <div class="sh-grid">
      <div class="sh-card full">
        <h3>Panic Card — the ten lines that matter</h3>
        <ul>
          <li>Read the whole paper once before writing anything.</li>
          <li>Answer what you know first — bank the marks early.</li>
          <li><b>Normalization:</b> kill partial dependencies, then transitive ones.</li>
          <li><b>ACID</b> — all-or-nothing, consistent, isolated, durable.</li>
          <li><b>B+ Tree:</b> data lives in the leaves; leaves are linked.</li>
          <li>Lost update? Lock it — two-phase locking.</li>
          <li>ER: box is an entity, diamond is a relation, oval is an attribute.</li>
          <li>Write something for every part-question. Partial marks count.</li>
          <li>Watch the clock — roughly 1.5 minutes per mark.</li>
          <li>You made a plan tonight. Trust it. Breathe.</li>
        </ul>
      </div>
      <div class="sh-card">
        <h3>Normalization ladder</h3>
        <ul>
          <li><b>1NF</b> — atomic values, no repeating groups</li>
          <li><b>2NF</b> — 1NF, plus no partial dependency on part of a key</li>
          <li><b>3NF</b> — 2NF, plus no transitive dependency</li>
          <li><b>BCNF</b> — every determinant is a superkey</li>
          <li>Anomalies on insert, update, or delete mean: normalize further</li>
        </ul>
      </div>
      <div class="sh-card">
        <h3>ACID &amp; transactions</h3>
        <ul>
          <li><b>Atomicity</b> — all steps or none</li>
          <li><b>Consistency</b> — valid state to valid state</li>
          <li><b>Isolation</b> — concurrent transactions never see half-work</li>
          <li><b>Durability</b> — a commit survives the crash</li>
          <li>States: active, then committed or aborted</li>
        </ul>
      </div>
      <div class="sh-card">
        <h3>B+ Trees &amp; indexing</h3>
        <ul>
          <li>Balanced — height is O(log n)</li>
          <li>Internal nodes hold keys only; data sits in the leaves</li>
          <li>Leaves are linked, so range scans are fast</li>
          <li>High fan-out means a shallow tree and fewer disk reads</li>
          <li>Strong for both equality and range queries</li>
        </ul>
      </div>
      <div class="sh-card">
        <h3>ER modeling</h3>
        <ul>
          <li>Rectangle — entity · diamond — relation</li>
          <li>Oval — attribute · underline — key</li>
          <li>Double line — total participation</li>
          <li>Cardinalities: 1:1, 1:N, M:N</li>
        </ul>
      </div>
      <div class="sh-card">
        <h3>Concurrency control</h3>
        <ul>
          <li><b>2PL</b> — a growing phase, then a shrinking phase; no releases in between</li>
          <li>2PL prevents lost updates and dirty reads</li>
          <li>Deadlock is a cycle of waits — detect, then abort one</li>
          <li>Timestamp ordering — the oldest transaction wins</li>
        </ul>
      </div>
    </div>
  </div>
</div>

<!-- ================= PANIC ================= -->
<div class="pn" id="pn">
  <button class="pn-x" id="pn-x">ESC · EXIT</button>
  <div class="pn-in">
    <div class="ring"><div class="core" id="core"><span id="ph-label">Breathe in</span><span id="ph-count">4</span></div></div>
    <p class="pn-msg">You have a plan. Just do the next small thing.</p>
    <p class="pn-cy" id="pn-cy">Box breathing · Cycle 1</p>
    <div class="pn-acts">
      <button class="btn btn-ghost" id="pn-sheet">Open Rescue Sheet</button>
      <button class="btn btn-ghost" id="pn-easy">Easiest next task</button>
      <button class="btn btn-primary" id="pn-calm">I'm calm now</button>
    </div>
  </div>
</div>

<script>
(function(){
'use strict';
const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];

/* ============ STATE ============ */
const KEY='skillos_mono_v1';
function fresh(){return{
  auth:null,remember:true,onboarded:false,ob:{},
  xp:320,level:4,conf:74,course:'CS302 · Databases',
  twin:{focus:'25 min',speed:'1.1×',retention:'68%',chrono:'Night',panic:'0.30'},
  blocks:[
    {id:1,title:'Normalization to BCNF',type:'Learn',minutes:25,xp:40,why:'6 of 8 past papers · avg 12 marks',cond:'Read the layered note, then mark it “I got it”.',state:'todo'},
    {id:2,title:'ACID Transactions — flashcards',type:'Recall',minutes:10,xp:25,why:'7 of 8 past papers · avg 10 marks',cond:'Answer 12 cards. Misses are tagged for the morning sweep.',state:'todo'},
    {id:3,title:'B+ Trees — the big picture',type:'Learn',minutes:20,xp:35,why:'5 of 8 past papers · avg 8 marks',cond:'Sketch one B+ tree from memory.',state:'todo'}
  ],
  islands:[
    {name:'SQL Foundations',status:'done',xp:50,mins:45,prereqs:[],concepts:['SELECT, WHERE, ORDER BY','Joins: inner and left','GROUP BY with HAVING'],checked:[true,true,true]},
    {name:'Relational Algebra',status:'done',xp:60,mins:40,prereqs:['SQL Foundations'],concepts:['Selection σ','Projection π','Join ⋈'],checked:[true,true,true]},
    {name:'ER Modeling',status:'done',xp:55,mins:35,prereqs:['Relational Algebra'],concepts:['Entities and relations','Keys and cardinality','Total vs partial participation'],checked:[true,true,false]},
    {name:'Normalization',status:'current',xp:70,mins:40,prereqs:['ER Modeling'],concepts:['Functional dependencies','1NF to BCNF, step by step','Anomalies and why they hurt'],checked:[false,false,false]},
    {name:'ACID Transactions',status:'locked',xp:65,mins:35,prereqs:['Normalization'],concepts:['ACID properties','Commit and rollback','Isolation levels'],checked:[false,false,false]},
    {name:'B+ Trees',status:'locked',xp:75,mins:45,prereqs:['ACID Transactions'],concepts:['Structure and fan-out','Leaf linking for ranges','Insert and split'],checked:[false,false,false]},
    {name:'Distributed Consensus',status:'locked',xp:90,mins:60,prereqs:['B+ Trees'],concepts:['Two-phase commit','Quorums','CAP in one line'],checked:[false,false,false]}
  ],
  material:false,plan:false,view:'dashboard'
}}
let S=load();
function load(){try{const s=JSON.parse(localStorage.getItem(KEY));if(s){
  if(!s.auth){const j=sessionStorage.getItem('skilos_session');if(j)s.auth=JSON.parse(j)}return s}}catch(e){}return fresh()}
function save(){try{const c={...S};if(!S.remember)c.auth=null;localStorage.setItem(KEY,JSON.stringify(c));
  if(!S.remember){S.auth?sessionStorage.setItem('skilos_session',JSON.stringify(S.auth)):sessionStorage.removeItem('skilos_session')}
  else sessionStorage.removeItem('skilos_session')}catch(e){}}

/* ============ SCREENS ============ */
function show(n){$$('.screen').forEach(s=>s.classList.remove('active'));$('#screen-'+n).classList.add('active');window.scrollTo(0,0)}
function examAt(){const d=new Date();d.setDate(d.getDate()+1);d.setHours(9,0,0,0);return d}
function countdown(){const ms=examAt()-new Date(),h=Math.max(0,Math.floor(ms/36e5)),m=Math.max(0,Math.floor(ms%36e5/6e4));
  const t=$('#cd-time');if(t)t.textContent=h+'h '+m+'m';
  const g=$('#greet-sub');if(g)g.textContent='Exam in '+h+'h '+m+'m — one block at a time.'}
setInterval(()=>{if($('#screen-app').classList.contains('active'))countdown()},30000);

/* ============ LOGIN ============ */
let tab='signin';
 $$('.tab').forEach(b=>b.addEventListener('click',()=>{tab=b.dataset.tab;
  $$('.tab').forEach(x=>x.classList.toggle('on',x===b));
  $('#f-name').hidden=tab!=='signup';$('#auth-go').textContent=tab==='signup'?'Create Account':'Sign In';note('')}));
function note(m){const n=$('#fnote');n.textContent=m;n.classList.toggle('show',!!m)}
 $('#peek').addEventListener('click',()=>{const p=$('#in-pass'),v=p.type==='password';
  p.type=v?'text':'password';$('#peek').textContent=v?'Hide':'Show'});
 $('#forgot').addEventListener('click',()=>{$('#auth-form').hidden=true;$('#fp-form').hidden=false});
 $('#fp-back').addEventListener('click',()=>{$('#auth-form').hidden=false;$('#fp-form').hidden=true;$('#fp-note').classList.remove('show')});
 $('#fp-form').addEventListener('submit',e=>{e.preventDefault();
  const v=$('#fp-email').value.trim();
  const n=$('#fp-note');
  if(!/^\S+@\S+\.\S+$/.test(v)){n.textContent='Enter a valid email so we can find the account.';n.classList.add('show');return}
  n.textContent='Reset link sent — check your inbox.';n.classList.add('show')});
 $('#auth-form').addEventListener('submit',e=>{e.preventDefault();
  const name=$('#in-name').value.trim(),email=$('#in-email').value.trim(),pass=$('#in-pass').value;
  if(tab==='signup'&&name.length<2){note('Please tell us your name.');return}
  if(!/^\S+@\S+\.\S+$/.test(email)){note('That email doesn\u2019t look right.');return}
  if(pass.length<6){note('Passwords need at least 6 characters.');return}
  const b=$('#auth-go');b.disabled=true;b.textContent=tab==='signup'?'Creating…':'Signing in…';
  setTimeout(()=>{b.disabled=false;b.textContent=tab==='signup'?'Create Account':'Sign In';
    S.remember=$('#remember').checked;
    S.auth={name:tab==='signup'?name:email.split('@')[0].replace(/[._-]/g,' ').replace(/\b\w/g,c=>c.toUpperCase()),email,demo:false};
    save();S.onboarded?app():startOB()},650)});
 $('#demo').addEventListener('click',()=>{S=fresh();
  S.auth={name:'Alex Chen',email:'alex@demo.sarvajna.app',demo:true};
  S.onboarded=true;S.material=true;S.plan=true;save();app()});

/* ============ ONBOARDING ============ */
const OB=[
 {q:'How much can you study per day?',s:'Be honest — the plan adapts to reality, not the ideal.',k:'hours',opts:['Under 1 hour','1–2 hours','2–4 hours','4+ hours'],two:1},
 {q:'Which subjects are you preparing?',s:'Pick everything that applies.',k:'subjects',multi:['Databases','Operating Systems','Networks','Algorithms','Mathematics','Electronics']},
 {q:'How do you learn best?',s:'Notes and quizzes are ordered around this.',k:'style',opts:['Visual diagrams','Reading notes','Practice problems','Video lessons'],two:1},
 {q:'How confident do you feel right now?',s:'No judgment. This seeds your Learning Twin.',k:'conf',slider:1},
 {q:'New concepts click when…',s:'We order every lesson accordingly.',k:'order',opts:['I see worked examples first','I read the theory first']},
 {q:'What is your focus pattern?',s:'It sets the length of your study sprints.',k:'focus',opts:['25-minute sprints','40-minute flow states','Marathon sessions','I drift quickly'],two:1},
 {q:'When is the exam — and the target?',s:'“Tomorrow” switches on Exam-Eve Rescue mode.',final:1}
];
let oi=0;const sel={subjects:[]};
function startOB(){oi=0;sel.subjects=[];show('onboarding');drawOB()}
function drawOB(){
  $('#ob-step').textContent='Calibration · Step '+(oi+1)+' of 7';
  $('#ob-fill').style.width=(oi/7*100)+'%';
  $('#ob-dots').innerHTML=OB.map((_,i)=>'<i class="'+(i<=oi?'on':'')+'"></i>').join('');
  const st=OB[oi],b=$('#ob-body');
  let h='<h3 class="ob-q">'+st.q+'</h3><p class="ob-s">'+st.s+'</p>';
  if(st.opts||st.multi)h+='<div class="opts'+(st.two?' two':'')+'">'+
    (st.opts||st.multi).map(o=>'<button class="opt" data-v="'+o+'"><span class="sq"></span>'+o+'</button>').join('')+'</div>';
  if(st.slider)h+='<div class="slider-w"><div class="sl-v" id="slv">50</div><div class="sl-w" id="slw">Getting there</div>'+
    '<input type="range" min="0" max="100" value="50" id="slr"/></div>';
  if(st.final)h+='<div class="ol" style="margin-bottom:10px">Exam timeline</div><div class="opts two">'+
    ['Tomorrow — rescue me','Within a week','This month','Long-term'].map(o=>'<button class="opt" data-t="'+o+'"><span class="sq"></span>'+o+'</button>').join('')+'</div>'+
    '<div class="ol" style="margin:22px 0 10px">Target score</div><div class="opts three">'+
    ['Just pass','60%','75%+'].map(o=>'<button class="opt" data-g="'+o+'"><span class="sq"></span>'+o+'</button>').join('')+'</div>';
  const needNext=st.multi||st.slider||st.final;
  h+='<div class="ob-foot"><button class="ob-back" id="ob-back"'+(oi===0?' style="visibility:hidden"':'')+'>Back</button>'+
    (needNext?'<button class="btn btn-primary" id="ob-next" '+(st.final?'disabled':'')+'>'
      +(st.final?'Build my Learning Twin':'Continue')+'</button>':'<span></span>')+'</div>';
  b.innerHTML=h;
  b.querySelectorAll('[data-v]').forEach(x=>x.addEventListener('click',()=>{
    if(st.multi){x.classList.toggle('sel');const v=x.dataset.v,i=sel.subjects.indexOf(v);
      i>=0?sel.subjects.splice(i,1):sel.subjects.push(v);$('#ob-next').disabled=!sel.subjects.length}
    else{b.querySelectorAll('[data-v]').forEach(y=>y.classList.remove('sel'));x.classList.add('sel');
      S.ob[st.k]=x.dataset.v;setTimeout(()=>{oi++;drawOB()},320)}}));
  b.querySelectorAll('[data-t]').forEach(x=>x.addEventListener('click',()=>{
    b.querySelectorAll('[data-t]').forEach(y=>y.classList.remove('sel'));x.classList.add('sel');sel.time=x.dataset.t;ready()}));
  b.querySelectorAll('[data-g]').forEach(x=>x.addEventListener('click',()=>{
    b.querySelectorAll('[data-g]').forEach(y=>y.classList.remove('sel'));x.classList.add('sel');sel.target=x.dataset.g;ready()}));
  function ready(){const n=$('#ob-next');if(n)n.disabled=!(sel.time&&sel.target)}
  const bk=$('#ob-back');if(bk)bk.addEventListener('click',()=>{oi--;drawOB()});
  const nx=$('#ob-next');
  if(nx&&!st.final)nx.addEventListener('click',()=>{if(st.slider)sel.conf=+$('#slr').value;oi++;drawOB()});
  if(nx&&st.final)nx.addEventListener('click',finish);
  const sl=$('#slr');
  if(sl)sl.addEventListener('input',()=>{const v=+sl.value;$('#slv').textContent=v;
    $('#slw').textContent=v<25?'Shaky':v<50?'Getting there':v<75?'Steady':'Confident'})}
function finish(){
  S.ob={hours:S.ob.hours||'2–4 hours',subjects:sel.subjects.length?sel.subjects:['Databases'],
    style:S.ob.style||'Practice problems',conf:sel.conf??50,order:S.ob.order||'I see worked examples first',
    focus:S.ob.focus||'25-minute sprints',timeline:sel.time,target:sel.target};
  S.twin={focus:{'25-minute sprints':'25 min','40-minute flow states':'40 min','Marathon sessions':'60 min','I drift quickly':'15 min'}[S.ob.focus]||'25 min',
    speed:S.ob.conf<40?'0.9×':S.ob.conf<70?'1.0×':'1.1×',
    retention:{'Under 1 hour':'52%','1–2 hours':'60%','2–4 hours':'68%','4+ hours':'74%'}[S.ob.hours]||'68%',
    chrono:/Tomorrow/i.test(S.ob.timeline)?'Night':'Morning',panic:/Tomorrow/i.test(S.ob.timeline)?'0.45':'0.25'};
  S.course=S.ob.subjects.includes('Databases')?'CS302 · Databases':S.ob.subjects[0];
  S.conf=Math.min(70,Math.round(34+S.ob.conf*.35));
  $('#ob-body').innerHTML='<div class="ob-done"><div class="ob-q" style="margin:0">Calibrating your Learning Twin</div><div class="ofl" id="ofl"></div></div>';
  const lines=[['Focus span',S.twin.focus],['Chronotype',S.twin.chrono],['Pace',S.twin.speed],['Twin v1','saved — it sharpens as you study']];
  const box=$('#ofl');
  lines.forEach((l,i)=>setTimeout(()=>{const d=document.createElement('div');
    d.innerHTML='<b>'+l[0]+'</b> — '+l[1];box.appendChild(d)},400+i*480));
  setTimeout(()=>{S.onboarded=true;save();app()},400+lines.length*480+800)}

/* ============ APP / HUD ============ */
const lvl=n=>Math.floor(n/100)+1;
function hud(){
  $('#nm-conf').textContent=S.conf+'%';$('#nm-level').textContent=S.level;$('#nm-xp').textContent=S.xp;
  $('#nm-course').textContent=S.course}
function gain(n){S.xp+=n;const l=lvl(S.xp);
  if(l>S.level){S.level=l;const ln=$('#lvl-note');
    ln.textContent='Level '+l+' — knowledge runs deeper.';ln.classList.add('show')}
  hud();save()}
function app(){show('app');hud();setView(S.view||'dashboard');countdown();
  const h=new Date().getHours(),g=h<12?'Good morning':h<17?'Good afternoon':'Good evening';
  $('#greet').textContent=g+', '+(S.auth?S.auth.name.split(' ')[0]:'Student')}
const TITLES={dashboard:'Dashboard',bringe:'Bringe Study',path:'Study Path',settings:'Settings'};
function setView(v){S.view=v;save();
  $$('.nl').forEach(n=>n.classList.toggle('on',n.dataset.view===v));
  $$('.view').forEach(x=>x.classList.toggle('active',x.id==='view-'+v));
  if(v==='dashboard'){flow();blocks();countdown()}
  if(v==='bringe')bringeSync();
  if(v==='path')drawPath();
  if(v==='settings')drawTwin()}
 $$('.nl').forEach(n=>n.addEventListener('click',()=>setView(n.dataset.view)));
document.addEventListener('click',e=>{const g=e.target.closest('[data-goto]');if(g)setView(g.dataset.goto)});

/* ============ FLOW ============ */
function flow(){
  const left=S.blocks.filter(b=>b.state==='todo'||b.state==='active').length;
  const s1=S.onboarded,s2=S.plan,cur=!s1?1:!s2?2:left?3:4;
  const steps=[['Calibrate','60-second setup'],['Plan','Pass Core only'],['Study','One block at a time'],['Pass','Sleep protected']];
  let h='';
  steps.forEach((s,i)=>{const n=i+1,cls=n<cur?'done':n===cur?'cur':'';
    h+='<div class="fs '+cls+'"><span class="fn">'+n+'</span><span class="fl">'+s[0]+'</span></div>';
    if(n===cur&&n===3)h+='<span class="here">you are here</span>';
    if(n===cur&&n===2)h+='<a class="go-link" data-goto="bringe" style="cursor:pointer">open Bringe Study</a>';
    if(i<3)h+='<span class="fc"></span>'});
  $('#flow-line').innerHTML=h}

/* ============ BLOCKS ============ */
function card(b,slot){
  if(slot==='now')return '<article class="block now" data-id="'+b.id+'">'+
    '<div class="ol">'+(b.state==='active'?'Now — in progress':'Now')+'</div>'+
    '<h3>'+b.title+'</h3><div class="bmeta">'+b.type+' · '+b.minutes+' min · +'+b.xp+' XP</div>'+
    '<p class="bwhy"><b>Why this:</b> '+b.why+'. It moves your score the most.</p>'+
    '<div class="bdone"><span>Done when</span>'+b.cond+'</div>'+
    '<div class="bacts">'+
    (b.state==='active'?'<button class="btn btn-primary" data-a="done">I got it — complete</button>'
      :'<button class="btn btn-primary" data-a="start">Start block</button>')+
    '<button class="btn btn-ghost" data-a="skip">Skip</button></div></article>';
  return '<div class="brow'+(slot==='later'?' later':'')+'" data-id="'+b.id+'">'+
    '<span class="ol">'+slot+'</span><div><b>'+b.title+'</b><i>'+b.type+' · '+b.minutes+' min · +'+b.xp+' XP</i></div>'+
    '<span class="st">'+(slot==='later'?'locked until next':'up next')+'</span></div>'}
function blocks(){
  const done=S.blocks.filter(b=>b.state==='done'||b.state==='skipped');
  const rest=S.blocks.filter(b=>b.state==='todo'||b.state==='active');
  $('#donetoday').innerHTML=done.length?'<div class="ol" style="margin-bottom:8px">Today</div>'+
    done.map(b=>'<div class="dt-row"><b>'+b.title+'</b><i>'+b.state+(b.state==='done'?' · +'+b.xp+' XP':'')+'</i></div>').join(''):'';
  if(!rest.length){$('#blocks').innerHTML='<div class="allclear">'+
    '<h3>Plan complete — go rest.</h3>'+
    '<p>Sleep is protected: 11:30 PM to 5:30 AM, six full hours. Do the thirty-minute morning sweep at 8:00, then walk in calm.</p>'+
    '<div class="bacts" style="justify-content:center">'+
    '<button class="btn btn-primary" data-sheet>Open Rescue Sheet</button>'+
    '<button class="btn btn-ghost" id="gn">Good night</button></div></div>';
    const g=$('#gn');if(g)g.addEventListener('click',()=>{g.textContent='Sleep locked — see you at 8:00 AM'});
    return}
  $('#blocks').innerHTML=card(rest[0],'now')+(rest[1]?card(rest[1],'Next'):'')+(rest[2]?card(rest[2],'Later'):'')}
 $('#blocks').addEventListener('click',e=>{const a=e.target.closest('[data-a]');if(!a)return;
  const b=S.blocks.find(x=>x.id===+a.closest('[data-id]').dataset.id);
  if(a.dataset.a==='start'){b.state='active';blocks();flow()}
  if(a.dataset.a==='done'){b.state='done';S.conf=Math.min(86,S.conf+4);gain(b.xp);blocks();flow()}
  if(a.dataset.a==='skip'){b.state='skipped';blocks();flow();save()}});

/* ============ BRINGE ============ */
const PC=[
 ['Normalization (1NF to BCNF)','6/8 papers · avg 12 marks · long-answer','Must','25 min · depth L2'],
 ['ACID & concurrency','7/8 papers · avg 10 marks · short and long','Must','25 min · depth L2'],
 ['B+ Trees & indexing','5/8 papers · avg 8 marks · numerical','Must','20 min · depth L1'],
 ['ER modeling','4/8 papers · avg 6 marks · diagram','Should','15 min · depth L1'],
 ['Relational algebra','3/8 papers · avg 5 marks · short','Should','15 min · depth L1']];
const TL=[
 ['8:32 PM','Learn · Normalization','25 min · depth L2',0],
 ['8:57 PM','Recall · ACID flashcards','10 min · mistakes tagged',0],
 ['9:07 PM','Break','10 min · stand, water, no phone',0],
 ['9:17 PM','Learn · B+ Trees','20 min · depth L1',0],
 ['9:37 PM','Build your Rescue Sheet','15 min · it writes itself as you learn',0],
 ['10:00 PM','Final sweep','30 min · Rescue Sheet + tagged misses',0],
 ['11:30 PM','Sleep — protected','6 hours · never cut, always earned',1],
 ['8:00 AM','Morning card','30 min · panic card + ten flashcards',0],
 ['9:00 AM','Exam — CS302','You walk in with a plan.',1]];
 $('#pcore').innerHTML=PC.map((p,i)=>'<div class="pc"><span class="rk">0'+(i+1)+'</span>'+
  '<div class="m"><b>'+p[0]+'</b><span>'+p[1]+'</span></div>'+
  '<div class="sd"><span class="tag'+(p[2]==='Must'?' solid':'')+'">'+p[2]+'</span><i>'+p[3]+'</i></div></div>').join('');
 $('#tline').innerHTML=TL.map(t=>'<div class="tl'+(t[3]?' protect':'')+'"><span class="t">'+t[0]+'</span>'+
  '<div class="b"><b>'+t[1]+'</b><span>'+t[2]+'</span></div>'+(t[3]&&t[0]==='11:30 PM'?'<span class="tag" style="flex:none">Protected</span>':'')+'</div>').join('');
function bringeSync(){
  $('#build').disabled=!S.material||S.plan;
  $('#build').style.display=S.plan?'none':'inline-flex';
  if(S.material&&!$('#dz').classList.contains('ok')&&!$('#dz').classList.contains('busy')){
    $('#dz').classList.add('ok');$('#dz').innerHTML='<div class="file-ok"><b>CS302_demo_syllabus.pdf</b><span>· 24 pages · 18 topics extracted</span></div>'}
  $('#result').hidden=!S.plan;countdown()}
 $('#dz').addEventListener('click',()=>$('#dz-input').click());
 $('#dz-input').addEventListener('change',e=>{if(e.target.files.length)ingest(e.target.files[0].name)});
 $('#demo-syl').addEventListener('click',e=>{e.stopPropagation();ingest('CS302_demo_syllabus.pdf')});
function ingest(name){
  const dz=$('#dz');dz.classList.add('busy');
  const st=document.createElement('p');st.className='status';dz.innerHTML='';dz.appendChild(st);
  const steps=[['Reading pages — 24 found',850],['Extracting 18 key topics',950],['Material ready',450]];
  let i=0;(function next(){if(i>=steps.length){dz.classList.remove('busy');dz.classList.add('ok');
      dz.innerHTML='<div class="file-ok"><b>'+name+'</b><span>· 24 pages · 18 topics extracted</span></div>';
      S.material=true;bringeSync();save();return}
    st.textContent=steps[i][0];setTimeout(next,steps[i][1])})()}
 $('#build').addEventListener('click',()=>{
  const b=$('#build'),st=$('#build-status');b.disabled=true;
  const steps=['Reading your material…','Scoring 18 topics against 8 past papers…','Selecting the Pass Core…','Protecting 6 hours of sleep…','Laying out the timeline…'];
  let i=0;(function next(){if(i>=steps.length){S.plan=true;bringeSync();save();flow();
      $('#result').hidden=false;$('#build-status').textContent='';return}
    st.textContent=steps[i++];setTimeout(next,820)})()});

/* ============ PATH ============ */
function drawPath(){
  $('#path').innerHTML=S.islands.map((is,i)=>{
    const cls=is.status;
    const inner=cls==='done'?'':'0'+(i+1);
    const st=cls==='done'?'Cleared':cls==='current'?'In progress':'Locked';
    return '<div class="isle '+cls+'" data-i="'+i+'"><div class="node '+cls+'">'+inner+'</div>'+
      '<b>'+is.name+'</b><i>'+is.mins+' min · +'+is.xp+' XP</i><em>'+st+'</em></div>'}).join('')}
 $('#path').addEventListener('click',e=>{const el=e.target.closest('.isle');if(el)openIsle(+el.dataset.i)});
function openIsle(i){
  const is=S.islands[i];
  const st=is.status==='done'?'Cleared':is.status==='current'?'In progress':'Locked';
  let cta;
  if(is.status==='current')cta='<button class="btn btn-primary" id="dw-done">Mark complete · +'+is.xp+' XP</button>';
  else if(is.status==='done')cta='<div class="dwnote">Island cleared. Revisit its concepts anytime before the exam.</div>';
  else cta='<div class="dwnote">Locked. Clear “'+S.islands[i-1].name+'” first — it is the prerequisite.</div>';
  $('#dw-panel').innerHTML='<button class="dw-x" id="dw-x">CLOSE</button>'+
    '<div class="ol">'+st+'</div><h3>'+is.name+'</h3><div class="st" style="color:var(--t3);font-size:13px">'+is.mins+' min · +'+is.xp+' XP</div>'+
    '<div class="dwsec"><h4>Prerequisites</h4>'+
    (is.prereqs.length?is.prereqs.map(p=>'<div class="dwrow"><span style="color:var(--t3)">cleared</span> — '+p+'</div>').join('')
      :'<div class="dwrow">None — this is your launchpad</div>')+'</div>'+
    '<div class="dwsec"><h4>Key concepts — tap to self-verify</h4>'+
    is.concepts.map((c,ci)=>'<button class="dwrow concept'+(is.checked[ci]?' on':'')+'" data-ci="'+ci+'"><span class="ck"></span>'+c+'</button>').join('')+'</div>'+
    '<div class="dwsec" style="margin-top:26px">'+cta+'</div>';
  $('#dw').classList.add('open');
  $('#dw-x').addEventListener('click',closeIsle);
  $('#dw-panel').querySelectorAll('.concept').forEach(c=>c.addEventListener('click',()=>{
    const ci=+c.dataset.ci;is.checked[ci]=!is.checked[ci];c.classList.toggle('on',is.checked[ci]);save()}));
  const d=$('#dw-done');
  if(d)d.addEventListener('click',()=>{is.status='done';
    const nx=S.islands.find(x=>x.status==='locked');if(nx)nx.status='current';
    gain(is.xp);drawPath();closeIsle()})}
function closeIsle(){$('#dw').classList.remove('open')}
 $('#dw-bg').addEventListener('click',closeIsle);

/* ============ SETTINGS ============ */
function drawTwin(){
  const r=[['Focus span',S.twin.focus],['Speed factor',S.twin.speed],['Retention estimate',S.twin.retention],
    ['Chronotype',S.twin.chrono],['Panic index',S.twin.panic]];
  $('#twin').innerHTML=r.map(x=>'<div class="kv"><span>'+x[0]+'</span><span class="dots"></span><b>'+x[1]+'</b></div>').join('')}
 $('#dl').addEventListener('click',()=>{
  const L='='.repeat(44);
  const txt=[L,' SARVAJNA · SKILLOS',' STUDENT INITIAL DATA — private export',L,
    'Generated : '+new Date().toLocaleString(),'Student   : '+(S.auth?S.auth.name:'—'),
    'Email     : '+(S.auth?S.auth.email:'—'),'Course    : '+S.course,'','CALIBRATION',
    '  Daily study bandwidth : '+(S.ob.hours||'2–4 hours'),
    '  Subjects              : '+((S.ob.subjects||['Databases']).join(', ')),
    '  Learning style        : '+(S.ob.style||'Practice problems'),
    '  Self-confidence       : '+((S.ob.conf??50))+' / 100',
    '  Concept grasp         : '+(S.ob.order||'Examples first'),
    '  Focus pattern         : '+(S.ob.focus||'25-minute sprints'),
    '  Exam timeline         : '+(S.ob.timeline||'Tomorrow (rescue)'),
    '  Target                : '+(S.ob.target||'Just pass'),'','LEARNING TWIN (v1)',
    '  Focus span     : '+S.twin.focus,'  Speed factor   : '+S.twin.speed,
    '  Retention est. : '+S.twin.retention,'  Chronotype     : '+S.twin.chrono,
    '  Panic index    : '+S.twin.panic,'','Pass confidence : '+S.conf+'% (estimate)',
    'XP / Level      : '+S.xp+' XP · Level '+S.level,'',
    'This file was generated locally. Your data stays yours.',L].join('\n');
  const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([txt],{type:'text/plain'}));
  a.download='student_initial_data.txt';a.click();URL.revokeObjectURL(a.href);
  const b=$('#dl'),t=b.textContent;b.textContent='Downloaded';setTimeout(()=>b.textContent=t,1600)});
let armed=false;
 $('#del').addEventListener('click',()=>{
  if(!armed){armed=true;$('#del').textContent='Tap again to confirm';
    setTimeout(()=>{armed=false;$('#del').textContent='Delete my Twin'},3000);return}
  const a=S.auth;S=fresh();S.auth=a;S.onboarded=false;save();startOB()});
 $('#recal').addEventListener('click',()=>{S.onboarded=false;save();startOB()});
function out(){localStorage.removeItem(KEY);sessionStorage.removeItem('skilos_session');
  S=fresh();show('login');
  $('#auth-form').hidden=false;$('#fp-form').hidden=true;note('')}
 $('#out').addEventListener('click',out);

/* ============ SHEET & PANIC ============ */
document.addEventListener('click',e=>{
  if(e.target.closest('[data-sheet]'))$('#sheet').classList.add('open');
  if(e.target.closest('[data-sheet-close]'))$('#sheet').classList.remove('open');
  if(e.target.closest('[data-panic]'))openPanic()});
 $('#print').addEventListener('click',()=>window.print());
const PH=[['Breathe in','grow'],['Hold','grow'],['Breathe out','shrink'],['Hold','shrink']];
let pT1=null,pT2=null,pCyc=1;
function openPanic(){$('#pn').classList.add('open');pCyc=1;cycle(0)}
function cycle(p){
  const ph=PH[p],core=$('#core');
  core.className='core '+ph[1];$('#ph-label').textContent=ph[0];
  let left=4;$('#ph-count').textContent=left;
  clearInterval(pT2);pT2=setInterval(()=>{if(--left>0)$('#ph-count').textContent=left},1000);
  clearTimeout(pT1);pT1=setTimeout(()=>{clearInterval(pT2);
    const n=(p+1)%4;if(n===0){pCyc++;$('#pn-cy').textContent='Box breathing · Cycle '+pCyc;
      if(pCyc===5)$('#pn-cy').textContent='Five cycles done — steadier? Exit anytime'}
    cycle(n)},4000)}
function closePanic(){$('#pn').classList.remove('open');clearTimeout(pT1);clearInterval(pT2)}
 $('#pn-x').addEventListener('click',closePanic);
 $('#pn-calm').addEventListener('click',closePanic);
 $('#pn-sheet').addEventListener('click',()=>{closePanic();$('#sheet').classList.add('open')});
 $('#pn-easy').addEventListener('click',()=>{closePanic();setView('dashboard')});

/* ============ BOOT ============ */
if(S.auth&&S.onboarded)app();
else if(S.auth)startOB();
else show('login');
countdown();
})();
</script>
</body>
</html>
